service:
  - tyoe: web
    name: cloudreve
    env: docker
    healthCheckPath: /
    envVars:
    - key: DB_USER
      # value:
    - key: DB_PASSWORD
      # value:
    - key: DB_NAME
      # value:
    - key: DB_HOST
      # value:
